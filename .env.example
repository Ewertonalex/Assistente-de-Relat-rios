# Obtenha chaves em: https://console.groq.com/
# Uma key (uso simples):
GROQ_API_KEY=sua_chave_aqui

# Várias keys (fallback automático em caso de rate limit) - use GROQ_API_KEYS com uma key por linha ou separadas por vírgula:
# GROQ_API_KEYS=chave1,chave2,chave3
